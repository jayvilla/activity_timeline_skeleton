export default function ActivityTimeline() {
  return (
    <div className="flex-1 p-4">
      {/* TODO: Call getEvents() with filters from URL/search params */}
      {/* TODO: Group events using groupEventsByDay() */}
      {/* TODO: Render day sections with ActivityRow for each event */}
      {/* TODO: Handle empty state (no events) */}
      <div className="text-center text-zinc-500 dark:text-zinc-400 py-8">
        Timeline content will be rendered here
      </div>
    </div>
  );
}

